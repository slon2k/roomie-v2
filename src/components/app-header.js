import React from 'react';

import Menu from './menu'

const AppHeader = () => {

    return (
        <div>
            <h1>Roomie</h1>
            <Menu/>
        </div>
    );
};

export default AppHeader;